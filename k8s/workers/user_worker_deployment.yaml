apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-worker
  namespace: yelp-prototype
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-worker
  template:
    metadata:
      labels:
        app: user-worker
    spec:
      containers:
        - name: user-worker
          image: 451978385896.dkr.ecr.us-east-1.amazonaws.com/yelp-prototype/user_worker:latest
          command: ["python", "workers/user_worker.py"]
          envFrom:
            - configMapRef:
                name: yelp-prototype-config
            - secretRef:
                name: yelp-prototype-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
