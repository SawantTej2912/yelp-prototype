apiVersion: apps/v1
kind: Deployment
metadata:
  name: restaurant-worker
  namespace: yelp-prototype
spec:
  replicas: 1
  selector:
    matchLabels:
      app: restaurant-worker
  template:
    metadata:
      labels:
        app: restaurant-worker
    spec:
      containers:
        - name: restaurant-worker
          image: 451978385896.dkr.ecr.us-east-1.amazonaws.com/yelp-prototype/restaurant_worker:latest
          command: ["python", "workers/restaurant_worker.py"]
          envFrom:
            - configMapRef:
                name: yelp-prototype-config
            - secretRef:
                name: yelp-prototype-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
